#include <stdio.h>

int main (void) {
	float v1;
	float v2;
	printf ("Valor 1: ");	
	scanf("%f",&v1);
	printf("Valor 2: ");
	scanf("%f",&v2);
	if (v1<v2) {
		printf("%f maior que %f\n", v2,v1);
		}
		else {
			printf("%f maior que %f\n", v1, v2);
		}
	return 0;
}
		
		
